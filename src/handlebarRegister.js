const fse = require('fs-extra');
const Handlebars = require('handlebars');

// Handlebars.registerHelper

function handlebarRegister(key, target, opts) {
	const {workDirectory} = opts;
	if (target) {
	}
}
